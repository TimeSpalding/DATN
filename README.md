Để chạy được hệ thống thì cần phải chạy Kaagle online để chạy móc nối từ weblocal của máy tính qua ngrok và nối với kaagle api, như vậy mới chạy được

lấy file kaagle có tên catvton-api-server chạy trên kaagle để chạy, nó sẽ tạo ra 1 đường dẫn online, sau đó mới bắt đầu chạy những file backend-frontend trong hệ thống
